{
  "name": "meeting-room-booking",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "concurrently -k -n API,WEB -c yellow,cyan \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "build": "pnpm --dir server build && pnpm --dir client build",
    "test": "pnpm --dir server test && pnpm --dir client test",
    "lint": "pnpm --dir server lint && pnpm --dir client lint",
    "typecheck": "pnpm --dir server typecheck && pnpm --dir client typecheck",
    "check": "pnpm --dir server check && pnpm --dir client check"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
